- As Python packages (python 3.7 >)
    - Server
        - install and run
            ```
            pip install opal-server
            ```
            ```
            opal-server
            ```

    - Client
        - install and run
            ```
            pip install opal-client
            ```
            ```
            opal-client
            ```